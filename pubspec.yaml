name: arctekko
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^2.14.1
  get_storage: ^1.1.1
  path_provider: ^1.6.11
  logger: ^0.9.1
  uuid: ^2.1.0

  get_database:
    path: ../get_database

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.10.0

flutter:
  uses-material-design: true